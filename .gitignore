*.db
*.db-shm
*.db-wal
# Cold-start scan caches (rebuilt per host from the local DB mtime; PA-local)
cache_funcwords.json
cache_hebnames.json
# Generated diagnostic dumps (entity-resolution build + tipnr import); regenerated each run
scripts/pn_binding_numonly.txt
scripts/pn_binding_hot.txt
scripts/pn_tier3_versification.txt
unmatched_pn.txt
__pycache__/
*.pyc
.claude/*
!.claude/commands/
.env
assets/figma-design/
assets/
node_modules/
.venv/
_commitmsg.txt
commitmsg.tmp
# Chronological build input (third-party reading-plan order; we commit only our
# generated source_oneyear.txt + static/chronological.json, not this raw input)
scripts/chronological/_source_raw.json
# Day-plan review artifact — regenerated by build_chronological.py each run
scripts/chronological/day_plan_preview.txt
# Word-gloss audit/build reports — regenerated by check_gloss_coverage.py / build_word_gloss.py
gloss_coverage_report.txt
gloss_*.tsv

# throwaway mobile design preview (handoff mockups, not the app)
design/_mobile_preview/

# Lexica draw cache — build_lexica_def.py's reviewed model draws (PA-only working artifacts,
# one file per word; the dry-run pass writes them, --apply ships them verbatim)
draws/
