# HANDOFF — lane ③: the B2 eyeball pass (100 rows), then the ②-ride

Written 2026-08-02 at lane ②'s close. Read CLAUDE.md first, then
`docs/tickets/TICKET_pn_star_fix.md` — that ticket is the ONLY authority for lane-②
state (predicate, ruling, pin, receipts, reconciliation). Do not re-derive from
TODO history or the spent `HANDOFF_pn_star_ride.md`.

## Lane ③ — the eyeball pass (own session, runs FIRST)

- **The list:** `python scripts/audit_pn_star_verb_merge.py --class B --list`, the B2
  block — **100 rows / 98 verses, canonical** (shared-splitter classification;
  supersedes the old 91 and the plan's 88, which proved a strict subset).
- **The job:** per-row adjudication against the ABP source (abp_texts/ is in the repo;
  anything byte-exact comes from a PA dump, not abp_texts): real merge the pinned
  TIPNR roster cannot see (gentilics — Sadducees/Romans/Ethiopians class) vs
  bracket-position artifact that is NOT a defect (`1Sa 25:42` — the name IS printed,
  on its own star). Every row gets a written disposition; reported, never dropped.
- **No auto-writes from this lane.** Any repair is hand-per-row, JP-checkpointed
  (splitter-B precedent). Bath-sheba is NOT in the pile — the fix pass auto-repairs
  those 5 (hyphen tokenization; see the ticket's reconciliation).
- Deliverable: a disposition table (ticket section or its own file) + whatever repair
  candidates survive, each with source evidence.

## The ②-ride (after ③; `/rebuild-words` as amended 8/1)

- Verdict-gated on THE PIN: build Results line **PN-star pass A 1,505 + B 2,552
  written / 3,161 refusals** (typed breakdown in the ticket); member record
  `plan_pn_star_corrected.txt` on PA, regenerable via `--plan --corrected`.
- Compare by MEMBER; counts are tripwires. Tail allowance derived fresh at the ride,
  every member named to its owning step.
- Regression pins: `tests/test_pn_star_merge_fix.py` (12) + the parser-level
  `test_pn_star_verb_merge.py` (source stays merged forever — that is not a failure).
- Post-ride reads: Mat 26:1 / Mat 27:26 / Gen 23:19 served state. **Mat 27:26 STAYS
  MERGED — legal typed refusal (rare-number class), reviewer-recorded. Not a miss.**
- Standing: rebuild never touches live (bible.db.new + one reversible swap);
  import_tipnr + roster regression + abp_surface floor + the retirement chain per the
  amended checklist; single-rollback rule (memory project_db_backups).

## Paste block for the reviewer chat

> Lexica, lane ③ — the PN-star B2 eyeball. Lane ② closed 2026-08-02 (your receipts:
> predicate + decision point + fallback rejection + pin acceptance + shared-splitter
> follow-up — record docs/tickets/TICKET_pn_star_fix.md). This session adjudicates
> the canonical 100-row roster-silent list per-row against the source: genuine
> merge the roster can't see vs bracket-position artifact. No auto-writes; repair
> candidates come back hand-per-row with evidence. After ③: the rebuild ride,
> verdict-gated on the pinned member picture (A 1,505 + B 2,552 / 3,161 typed).
