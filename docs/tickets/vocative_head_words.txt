VOCATIVE "O" AS THE DISPLAYED NAME — CLASS TICKET (opened 2026-07-28, JP sighting Jer 15:5;
RE-SCOPED 2026-07-29 on the live read; FIX SHIPPED same session)

SIGHTING: Jer 15:5 word card for Jerusalem shows "O" on the English name line (headword
Ιερουσαλήμ itself correct). Reproduced live 2026-07-29 before fixing.

ROOT CAUSE — CORRECTED HISTORY (#90 re-read earned its keep, third time that session):
  * ORIGINAL claim (2026-07-28, WRONG): build-side _head_word() skips only the/a/an, so
    "O" wins english_head for every "O <word>" slot; class sized ~1,240 from a SOURCE
    scan. Wrong on BOTH axes — wrong count (live: 1,520 rows begin "O ") and wrong LAYER:
    the live english_head data is CLEAN ("jerusalem", "LORD", "son", ...; JP live paste
    2026-07-29). A later build pass re-cleans heads; the ticket's build-side reasoning was
    never checked against the load path. Zero data repair needed.
  * REAL root cause (proven live): frontend display picker extractProperName()
    (static/src/00-core.jsx:417) takes the first capitalized word of the English not in
    _PN_STOP — "O" was not in the stop list, so "O Jerusalem?" -> "O".
  * HALF THE DEFECT IS NOT COSMETIC: the same extracted name feeds the person/place
    identity lookups, so a vocative name slot queried "O" and came back empty — the
    card's "no Strong's mapping" state on such slots may be THIS defect, not the data.

FIX (one word + comment, 00-core.jsx): "O" added to _PN_STOP. Collision-proofed by JP
live reads before the edit: english_head='O' count 0 · is_pn bare-"O" count 0. Edge
stated: ~200 article slots glossed exactly "O" now extract NO name instead of "O" —
every consumer is a name path, so empty is the honest value there.

BLAST RADIUS — the ten call sites, all through the one function (static/src):
  30-detail-panel.jsx :343 (gentilic test) · :376 (identity lookup name) · :478 ·
  :513 · :516 · :566 (entity/name resolution paths) · :705 (card properName) ·
  :854 (click name) · :1192 (name-search occ link) · 00-core.jsx:417 (the definition)

RECEIPTS OWED POST-DEPLOY (reviewer-ruled):
  1. Jer 15:5 card name line reads "Jerusalem" (known positive).
  2. EXPLICIT identity-lookup check on that card — if "no Strong's mapping" persists,
     that is a FINDING with a second cause: gets its own ticket line, not absorbed.
  3. One at-scale sample: an "O LORD" slot (480 instances) renders correctly.

STATUS: fix committed, awaiting deploy + live receipts.
